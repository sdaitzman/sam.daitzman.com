{
  "name": "code",
  "version": "1.0.0",
  "description": "",
  "main": "scripts.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel watch *.html -d docs & cd docs && http-server -c 0.25 -p 1234",
    "build": "parcel build *.html -d docs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "draggabilly": "^2.2.0",
    "normalize.css": "^8.0.0",
    "prefixfree": "^1.0.0",
    "sweetalert": "^2.1.0"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "parcel-bundler": "^1.11.0"
  }
}
